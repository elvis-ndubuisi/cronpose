import { GeistSans, GeistMono } from "geist/font";
import { Inter } from "next/font/google";

const inter = Inter({
	subsets: ["latin"],
});

export { GeistSans as geist, GeistMono as geistMono, inter };
